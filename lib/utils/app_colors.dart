import 'package:flutter/material.dart';

class AppColors {
  // Couleurs Primaires & Secondaires
  static const Color bleuStock = Color(0xFF1B4F8B);    // Action principale
  static const Color grisMaster = Color(0xFF5E666D);   // Éléments secondaires

  // Couleur d'Accentuation
  static const Color vertCroissance = Color(0xFF5CB85C); // Validation, Ajout

  // Couleurs Sémantiques (États)
  static const Color rougeErreur = Color(0xFFD9534F);    // Rupture de stock
  static const Color orangeAlerte = Color(0xFFF0AD4E);   // Stock faible

  // Couleurs Neutres & Fonds
  static const Color blancFond = Color(0xFFFFFFFF);
  static const Color grisClair = Color(0xFFF4F6F8);      // Fond des écrans
  static const Color grisFonce = Color(0xFF2C3238);      // Texte principal
}